# تطبيق تفريغ محتوى يوتيوب | YouTube Transcript App

تطبيق Flask بسيط وفعال لتفريغ وتلخيص محتوى مقاطع اليوتيوب مع التكامل الكامل مع n8n.

## 📁 هيكل المشروع

```
windsurf-project/
├── app.py                 # 🎥 YouTube Transcript - تفريغ محتوى يوتيوب

نظام متكامل لتفريغ محتوى فيديوهات يوتيوب إلى نص مكتوب مع ملخص ذكي وشات بوت للنقاش باستخدام الذكاء الاصطناعي.

## ✨ المميزات

- 🎯 **تفريغ تلقائي**: تحويل أي فيديو يوتيوب إلى نص مكتوب
- 🤖 **ملخص ذكي**: ملخص تلقائي مع أهم النقاط باستخدام GPT-4
- 💬 **شات بوت**: نقاش المحتوى مع AI مدعوم بـ vector store
- 📁 **حفظ تلقائي**: جميع التفريغات تُحفظ في قاعدة بيانات
- 📄 **تصدير PDF/Word**: تصدير النتائج بصيغ مختلفة
- 👤 **نظام مستخدمين**: تسجيل دخول وحسابات شخصية
- 🎨 **تصميم يوتيوب**: واجهة احترافية بألوان وتصميم يوتيوب

## 🛠️ التقنيات المستخدمة

### Backend
- **Flask**: إطار عمل Python للـ backend
- **SQLAlchemy**: ORM لقاعدة البيانات
- **Flask-Login**: نظام المصادقة
- **SQLite**: قاعدة بيانات خفيفة

### Frontend
- **HTML/CSS**: واجهة المستخدم
- **TailwindCSS**: تنسيق الواجهة
- **JavaScript**: التفاعل والديناميكية
- **html2pdf.js**: تصدير PDF

### AI & Automation
- **n8n**: أتمتة workflow
- **OpenAI GPT-4**: الذكاء الاصطناعي
- **Vector Store**: تخزين المحتوى للشات بوت

## 📋 المتطلبات

- Python 3.8+
- n8n instance (للـ workflows)
- OpenAI API Key

## 🚀 التثبيت

### 1. Clone المشروع
```bash
git clone https://github.com/Mrsos07/youtubetotext.git
cd youtubetotext
```

### 2. تثبيت المكتبات
```bash
pip install -r requirements.txt
```

### 3. إعداد n8n Workflows
- استورد workflows من مجلد `n8n_workflows/`
- أضف OpenAI API Key في n8n
- حدّث webhook URLs في `app.py`

### 4. إنشاء مستخدم Admin
```bash
python create_admin.py
```

**بيانات الدخول الافتراضية:**
- Email: `admin@admin.com`
- Password: `Ss@0011`

### 5. تشغيل التطبيق
```bash
python app.py
```

التطبيق سيعمل على: `http://localhost:5000`

### 6. نشر بالحاوية (Docker)

```bash
# بناء الصورة
docker build -t youtube-transcript .

# التشغيل مع تحميل متغيرات البيئة من ملف .env وفتح المنفذ 5000
docker run --env-file .env --rm -p 5000:5000 youtube-transcript
```

تأكد من ملئ قيم `SECRET_KEY`, `N8N_WEBHOOK_URL`, و `N8N_CHAT_WEBHOOK_URL` في ملف `.env` (يمكن نسخ `.env.example`).

### 7. رفع المشروع إلى GitHub

```bash
git add .
git commit -m "Prepare for Render deployment"
git push origin main
```

بعد الرفع، اربط المشروع بـ Render عبر Blueprint أو ربط الخدمة في `render.yaml`.

## 📁 هيكل المشروع

```
youtubetotext/
├── app.py                 # التطبيق الرئيسي
├── models.py             # نماذج قاعدة البيانات
├── requirements.txt      # المكتبات المطلوبة
├── create_admin.py       # إنشاء مستخدم admin
├── templates/            # صفحات HTML
│   ├── index.html       # الصفحة الرئيسية
│   ├── login.html       # تسجيل الدخول
│   ├── register.html    # التسجيل
│   └── my_files.html    # ملفاتي
└── n8n_workflows/       # ملفات n8n
```

## 🎯 كيفية الاستخدام

### 1. تسجيل الدخول
- افتح `http://localhost:5000`
- سجل دخولك بحساب admin أو أنشئ حساب جديد

### 2. تفريغ فيديو
- أدخل رابط فيديو يوتيوب
- اضغط "ابدأ التفريغ"
- انتظر النتائج (قد تستغرق بضع دقائق)

### 3. عرض النتائج
- المقدمة
- الملخص
- أهم النقاط
- المحتوى الكامل

### 4. الشات بوت
- اسأل أي سؤال عن محتوى الفيديو
- سيرد البوت بناءً على المحتوى المحفوظ

### 5. ملفاتي
- اذهب إلى "ملفاتي"
- عرض جميع التفريغات السابقة
- تصدير PDF أو عرض التفاصيل

## 🔧 الإعدادات

### تغيير webhook URLs
في `app.py`:
```python
N8N_WEBHOOK_URL = "your-n8n-webhook-url"
N8N_CHAT_WEBHOOK_URL = "your-chat-webhook-url"
```

### تغيير Secret Key
في `app.py`:
```python
app.config['SECRET_KEY'] = 'your-secret-key-here'
```

## 📝 n8n Workflows

المشروع يحتاج إلى workflow واحد في n8n:

### YouTube Transcript Workflow
- **Webhook Trigger**: يستقبل رابط اليوتيوب
- **YouTube Transcript**: يجلب النص من الفيديو
- **OpenAI**: يولد الملخص والنقاط الرئيسية
- **Vector Store**: يحفظ المحتوى للشات بوت
- **Respond to Webhook**: يرجع النتائج

### Chat Workflow
- **Chat Trigger**: يستقبل رسائل المستخدم
- **AI Agent**: يرد بناءً على المحتوى المحفوظ
- **Vector Store**: يبحث في المحتوى

## 🐛 حل المشاكل

### المشكلة: لا تظهر النتائج
**الحل**: تأكد من:
- n8n workflow يعمل بشكل صحيح
- "Respond to Webhook" node متصل في نهاية workflow
- webhook URLs صحيحة في `app.py`

### المشكلة: الشات بوت لا يرد
**الحل**: تأكد من:
- Chat workflow مفعّل في n8n
- Vector store يحتوي على البيانات
- webhook URL صحيح

## 📄 الترخيص

صُنع بإتقان © 2025

## 👨‍💻 المطور

تم تطوير هذا المشروع بواسطة Mrsos07

## 🤝 المساهمة

المساهمات مرحب بها! يرجى فتح issue أو pull request.

## 📧 التواصل

للأسئلة والاستفسارات، يرجى فتح issue في GitHub.

## 🚀 التثبيت والتشغيل

### 1. تثبيت المكتبات
```bash
pip install -r requirements.txt
```

### 2. تشغيل التطبيق
```bash
python app.py
```

التطبيق سيعمل على: **http://localhost:5000**

## 🔗 إعدادات n8n

الـ webhook URL المستخدم:
```
https://n8n.srv968786.hstgr.cloud/webhook/youtube_text
```

**مهم:** الـ webhook يجب أن يكون مضبوط على **GET** method في n8n.

## ✨ المميزات

- ✅ تفريغ تلقائي لمحتوى يوتيوب
- ✅ ملخص ذكي مع أهم النقاط
- ✅ عرض المحتوى الكامل
- ✅ واجهة عصرية وسهلة الاستخدام
- ✅ تصدير النتائج
- ✅ لا توجد مشاكل CORS

## 🎯 كيفية الاستخدام

1. افتح المتصفح على `http://localhost:5000`
2. أدخل رابط يوتيوب
3. اضغط "تفريغ المحتوى"
4. انتظر النتائج (قد تستغرق بضع دقائق)

## 🛠️ التقنيات المستخدمة

- **Backend**: Flask (Python)
- **Frontend**: HTML + TailwindCSS
- **Integration**: n8n webhooks
- **AI**: OpenAI GPT-4 (via n8n)

## 📝 ملاحظات

- الـ timeout للطلبات: 5 دقائق
- يدعم جميع روابط يوتيوب
- يعمل مع n8n workflow الموجود

## 🔧 نشر على Render

### الخطوات:

1. **تأكد من أن جميع الملفات محدثة**:
   - `requirements.txt` يحتوي على `psycopg2-binary`
   - `render.yaml` محدث بإعدادات قاعدة البيانات
   - `app.py` يحتوي على migration للأعمدة الجديدة

2. **ارفع التغييرات إلى GitHub**:
```bash
git add .
git commit -m "Fix database migration for Render deployment"
git push origin main
```

3. **في Render Dashboard**:
   - سيتم إنشاء قاعدة بيانات PostgreSQL تلقائياً
   - سيتم ربطها بالتطبيق عبر `DATABASE_URL`
   - التطبيق سيقوم بإنشاء الجداول وإضافة الأعمدة المفقودة تلقائياً

### حل مشكلة SQLAlchemy:
التحديث الأخير أضاف:
- Migration تلقائي للأعمدة الجديدة (`reset_token`, `reset_token_expiry`)
- دعم PostgreSQL عبر `psycopg2-binary`
- إعداد صحيح لقاعدة البيانات في `render.yaml`

---

**صنع بـ ❤️ باستخدام Flask و n8n**
